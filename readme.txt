Salut 👋,

Je vais t'expliquer comment configurer le bot pour qu'il fonctionne sans problème ! 🚀
Tout d'abord, tu vas devoir te rendre dans le fichier config.json. ATTENTION, il y a 3 fichiers à configurer : un dans commands, un dans events, et un dans le dossier principal.

Tu devras renseigner le token de ton bot, le client ID et le guild ID dans les 3 fichiers ! 🔐

Une fois que c'est fait, lance le bot en exécutant node index.js ! 🎉


--------------------------------------------------------------------------------------------------------------

Hi there 👋,

Let me explain how to set up the bot so it works without any issues! 🚀
First, you'll need to go to the config.json file. WARNING, there are 3 files to configure: one in commands, one in events, and one in the main folder.

You'll need to enter your bot's token, client ID, and guild ID in all 3 files! 🔐

Once that's done, run the bot by executing node index.js! 🎉